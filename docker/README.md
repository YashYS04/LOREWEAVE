# docker/README.md

## Docker Configuration Notes

This directory contains supplemental Docker artefacts.

The primary `docker-compose.yml` lives at the project root.

### Files

| File | Purpose |
|---|---|
| (none yet) | Supplemental compose overrides will be placed here |

### Overrides (future)

Use `docker-compose.override.yml` for local developer overrides that should
not be committed (bind-mount volumes, debug ports, etc.).
