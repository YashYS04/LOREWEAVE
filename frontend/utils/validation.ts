/**
 * Shared Zod validation schemas.
 *
 * Place schemas that are reused across multiple features here. Feature-specific
 * schemas live alongside the feature (e.g., features/foo/foo.schema.ts).
 */
export { z } from "zod";
