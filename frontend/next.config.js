/** @type {import('next').NextConfig} */
const nextConfig = {
  // Strict mode surfaces extra React warnings in development.
  reactStrictMode: true,

  // Keep Next.js server actions minimal — no external domains needed yet.
  experimental: {},
};

module.exports = nextConfig;
